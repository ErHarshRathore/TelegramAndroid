package org.telegram.ui.Profile;


public class TypeTransformer {
    protected static org.telegram.ui.ProfileActivity.AvatarImageView transformAvatarImageView(
            org.telegram.ui.ProfileActivity.AvatarImageView avatarImageView
    ) {
        // This method can be used to transform the AvatarImageView if needed
        return avatarImageView;
    }
}
